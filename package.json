{
  "name": "irdb-rag",
  "version": "0.1.0",
  "scripts": {
    "build:css": "mkdir -p target/site && npx tailwindcss -i ./src/web_app/style/tailwind.css -o ./target/site/tailwind.css --minify",
    "watch:css": "mkdir -p target/site && npx tailwindcss -i ./src/web_app/style/tailwind.css -o ./target/site/tailwind.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0",
    "postcss": "^8.4.0",
    "autoprefixer": "^10.4.0"
  }
}
